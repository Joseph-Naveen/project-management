# Example Backend Environment Variables for Render
# Set these in your Render Web Service dashboard under Environment tab

# Server Configuration (PORT is automatically set by Render)
NODE_ENV=production

# Database Configuration (Get these from your Render PostgreSQL service)
DB_HOST=dpg-xxxxx-a.oregon-postgres.render.com
DB_PORT=5432
DB_NAME=project_management_db
DB_USER=project_user
DB_PASSWORD=your_secure_db_password
ADMIN_PASSWORD=SecureAdmin123!

# JWT Configuration (Generate strong secrets)
JWT_SECRET=a1b2c3d4e5f6789012345678901234567890abcdef1234567890
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration (Set to your frontend URL)
CORS_ORIGIN=https://your-frontend-service.onrender.com

# Email Configuration (Optional - for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password
SMTP_FROM=noreply@yourproject.com

# Security
BCRYPT_ROUNDS=12
SESSION_SECRET=9876543210fedcba098765432109876543210abcdef

# Rate Limiting (Optimized for free tier)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info

# Redis Configuration (Optional - if using Redis service)
# REDIS_HOST=your-redis-host.onrender.com
# REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password

# File Upload (Uses local filesystem on Render)
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,application/pdf,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document
